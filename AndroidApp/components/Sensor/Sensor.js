import React from 'react'
import {View, Text} from 'react-native'
import { styles } from './Sensor.component.style.android'

const Sensor = ({navigation, route}) => {
    return (
        <View>
            <Text>Sensor + {styles}</Text>
        </View>
    )
  }

  export default Sensor